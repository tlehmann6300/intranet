/**
 * app.js – Vite entry point
 *
 * Import global JavaScript modules here.  Vite will bundle, tree-shake
 * and fingerprint the output, writing it to `assets/dist/`.
 *
 * Example:
 *   import './modules/dark-mode.js';
 *   import './modules/toast.js';
 *
 * Existing inline scripts inside PHP templates can be migrated here
 * incrementally.
 */

// Navbar scroll behaviour (migrated from /js/navbar-scroll.js)
import '../../../js/navbar-scroll.js';
